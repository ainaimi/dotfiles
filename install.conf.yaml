- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.gitconfig:
    ~/.zshrc:
    ~/.config/bat:
        create: true
        path: config/bat

- create:
    - ~/Work

- shell:
    - 
        command: ./setup_homebrew.zsh
        stdout: true
        stderr: true
